# Hygiene Inspection — 2026-07-18

**Status:** approved
**Issues found:** 4
**Created:** 2026-07-18 23:30:00
**Approved by:** Julius
**Approved on:** 2026-07-20
**Validator:** hygiene-inspector
**Paths checked:** 51,889

---

## Summary

| Severity | Count |
|---|---|
| ERROR | 2 |
| WARNING | 1 |
| INFO | 1 |

**Result:** Identical to 07-15, 07-16, and 07-17 baseline. No new violations, no regressions, no resolutions. Fourth consecutive run with the same 4 issues.

---

## Issue 1: Recurring root folder `memory/`

**Path:** `memory/`
**Severity:** ERROR
**Category:** Orphan
**Issue:** Recurring root folder not in whitelist — 10th occurrence since 07-03
**Current:** `memory/` directory at KB root, contains `2026-07-15.md`
**Expected:** `memory/` was migrated to `.openclaw/memory/` in folder-structure.md v1.2 (2026-05-17)
**Suggested fix:** Move `memory/2026-07-15.md` to `.openclaw/memory/`, then `rmdir memory/`. Identify and fix the writing process that targets `memory/` instead of `.openclaw/memory/` — file deletion is transient without a process fix.

---

## Issue 2: Recurring root folder `state/`

**Path:** `state/`
**Severity:** ERROR
**Category:** Orphan
**Issue:** Recurring root folder not in whitelist — 6th recurrence since original 06-25 resolution
**Current:** Empty `state/` directory at KB root
**Expected:** No `state/` folder at root. If runtime state is needed, it belongs in `.hermes/` or `.openclaw/`
**Suggested fix:** `rmdir state/`. Identify and fix the process that recreates an empty `state/` at KB root — deletion is transient without a process fix.

---

## Issue 3: Orphan file inside non-whitelisted root folder

**Path:** `memory/2026-07-15.md`
**Severity:** WARNING
**Category:** Path
**Issue:** File inside non-whitelisted root folder `memory/`
**Current:** `memory/2026-07-15.md` — not classified by any rule
**Expected:** Memory files should be in `.openclaw/memory/`
**Suggested fix:** Move to `.openclaw/memory/2026-07-15.md`, then remove `memory/` directory

---

## Issue 4: Empty directory `state/`

**Path:** `state/`
**Severity:** INFO
**Category:** Orphan
**Issue:** Empty directory
**Current:** `state/` is an empty directory
**Expected:** Non-empty directory or removed
**Suggested fix:** `rmdir state/` (redundant with Issue 2)

---

## Delta Analysis

| Metric | 07-15 | 07-16 | 07-17 | 07-18 |
|---|---|---|---|---|
| Total issues | 4 | 4 | 4 | 4 |
| ERRORs | 2 | 2 | 2 | 2 |
| WARNINGs | 1 | 1 | 1 | 1 |
| INFOs | 1 | 1 | 1 | 1 |
| Paths checked | 51,845 | 51,861 | 51,883 | 51,889 |

**Delta vs 07-17:** 0 change in issue counts. Paths checked +6 (from this report and action file writes).

**Four-day trend:** Fully stable. No new structural violations since the 07-14 clean baseline was broken on 07-15. Both `memory/` and `state/` remain unaddressed — each deletion attempt has been transient. The root cause is process-level: something writes to `memory/` and something creates `state/` at KB root.

---

## Process-level fix needed

Both recurring issues have persisted through multiple Fix Agent deletion attempts:

- **`memory/`**: 10th occurrence since 07-03. A process (likely a cron job or agent) writes memory files to `memory/` instead of `.openclaw/memory/`. The writer's output path must be corrected.
- **`state/`**: 6th recurrence since 06-25. A process creates an empty `state/` at KB root. The creator must be identified and its output path corrected.

Until the root causes are fixed at the process level, these issues will continue to reappear in every hygiene run.

---

*Report generated by Hermes — Hygiene Inspector v1.12*
