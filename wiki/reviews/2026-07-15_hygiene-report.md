# Hygiene Inspection — 2026-07-15

**Status:** approved
**Issues found:** 4 (2 ERROR, 1 WARNING, 1 INFO)
**Created:** 2026-07-15 23:30
**Approved by:** Julius
**Approved on:** 2026-07-20
**Validator:** hygiene-inspector

**Paths checked:** 51,845

---

## Issue 1: Recurring root folder `memory/`

**Path:** `memory/`
**Severity:** ERROR
**Category:** Orphan
**Issue:** Recurring root folder not in whitelist. `memory/` was migrated to `.openclaw/memory/` in v1.2 but keeps reappearing (flagged 07-03, 07-06, 07-07, 07-08, 07-11, now 07-15).
**Current:** `memory/` directory at root containing `2026-07-15.md` (user content file)
**Expected:** Memory log files belong in `.openclaw/memory/`, not at KB root. `.openclaw/memory/2026-07-15.md` already exists at 2,665 bytes.
**Suggested fix:** Move `memory/2026-07-15.md` to `.openclaw/memory/` (or merge with existing), then `rmdir memory/`. Identify and fix the writing process that targets `memory/` instead of `.openclaw/memory/`. This is a **process-level leak** — file deletion alone will not resolve it permanently.

---

## Issue 2: Recurring root folder `state/`

**Path:** `state/`
**Severity:** ERROR
**Category:** Orphan
**Issue:** Recurring root folder not in whitelist. Previously resolved 2026-06-27, recreated 2026-07-02. Present again 2026-07-15.
**Current:** Empty directory at root (0 files)
**Expected:** No `state/` directory at KB root. If needed, move inside `.hermes/` or `.openclaw/`.
**Suggested fix:** `rmdir state/`. If a process recreates it, identify and fix the process output path.

---

## Issue 3: Orphan file inside `memory/`

**Path:** `memory/2026-07-15.md`
**Severity:** WARNING
**Category:** Path
**Issue:** File inside non-whitelisted root folder `memory/`. This file should be in `.openclaw/memory/`, not in `memory/` at KB root.
**Current:** `memory/2026-07-15.md` (2,665 bytes)
**Expected:** Memory log files belong in `.openclaw/memory/`
**Suggested fix:** Move to `.openclaw/memory/2026-07-15.md` (or merge if already exists). Delete `memory/` directory afterward.

---

## Issue 4: Empty directory `state/`

**Path:** `state/`
**Severity:** INFO
**Category:** Orphan
**Issue:** Empty directory at KB root
**Current:** `state/` — empty (0 files, 0 subdirectories)
**Expected:** Non-empty directory or removed
**Suggested fix:** `rmdir state/`

---

## Summary

| Severity | Count |
|---|---|
| ERROR | 2 |
| WARNING | 1 |
| INFO | 1 |
| **Total** | **4** |

### Recurring issues

Both `memory/` and `state/` are **recurring root folders** that have been flagged multiple times since late June/early July. These need process-level fixes:

- **`memory/`**: A process writes memory logs to `memory/` instead of `.openclaw/memory/`. The output path in that process must be corrected.
- **`state/`**: A process recreates an empty `state/` directory at KB root. The process must be updated to use `.hermes/` or `.openclaw/` for state storage.

### Compared to previous run (07-14)

- 07-14: 0 issues (clean) across 51,831 paths
- 07-15: 4 issues across 51,845 paths (+14 paths from new content)
- Both `memory/` and `state/` reappeared after being resolved on 07-14

### Compared to 07-13

- 07-13: `state/` flagged (resolved 07-14), `memory/` not present
- 07-15: Both `state/` AND `memory/` present — regression from clean 07-14 baseline

---

*Report generated by hygiene-inspector v1.11*
